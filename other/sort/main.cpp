#include <iostream>

#define N 1001

using namespace std;

int main()
{
    int n, st[N];
    cout << "各种排序：" << endl;
    while (cout << "请输入要排序的数的个数：", cin >> n, n)
    {
        cout << "请输入要排序的数：" << endl;
        for (i = 0; i < n; ++ i)
            cin >> st[i];
        cout << "排序方法：" << endl;
        cout << ""
    }
    return 0;
}
